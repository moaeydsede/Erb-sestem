# ERP محاسبة + تصنيع + مراكز كلفة (Frontend فقط) — النسخة النهائية v5

## تسجيل الدخول (افتراضي)
- Admin: `admin / admin123`
- Accountant: `accountant / acc123`
- Viewer: `viewer / view123`

## صلاحيات
- Admin: كل شيء + الإعدادات + دليل الحسابات (إضافة/حذف)
- Accountant: تشغيل كامل بدون الإعدادات (وقيود دليل الحسابات محمية)
- Viewer: عرض فقط (Dashboard + Reports)

## إضافات v5
- كشف حساب عميل/مورد + طباعة PDF
- رصيد افتتاحي للعملاء والموردين (من البيانات الأساسية → زر "رصيد افتتاحي")
- قفل فترات محاسبية (من الإعدادات → "قفل الفترة حتى تاريخ") يمنع القيود/الحذف قبل/حتى تاريخ القفل
- صلاحيات (Admin / Accountant / Viewer)
- شاشة دخول بدون ملاحظات

## النشر على GitHub Pages
ارفع الملفات في الريبو ثم:
Settings → Pages → Deploy from branch → main → /root


## Excel
- تصدير Excel من القائمة ☰ → البيانات → (تصدير Excel)
- استيراد Excel من نفس المكان (استيراد Excel) — يفضّل أخذ Backup JSON أولاً.

## الطباعة PDF
زر 🖨️ PDF في صفحة التقارير يدعم:
- كشف حساب عميل/مورد
- دفتر الأستاذ
- ميزان مراجعة
- أرباح وخسائر
- تقرير مركز كلفة
